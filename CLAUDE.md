# kaitori-post

買取商品の記事（ブログ）・SNS投稿を、Claude Code のサブエージェントチームで半自動生成するプロジェクト。

## エージェントチーム構成

| エージェント | 役割 | 主な入出力 |
| --- | --- | --- |
| `kaitori-researcher` | 相場・スペック・需要のリサーチ | CSV/テキスト → `output/research/` |
| `kaitori-blog-writer` | SEOブログ記事の執筆 | research → `output/blog/` |
| `kaitori-sns-writer` | X / Instagram 投稿の執筆 | research/blog → `output/sns/` |
| `kaitori-editor` | 校正・法令・SEOチェック＆修正 | 原稿 → 上書き修正 + `output/review/` |

## 標準ワークフロー

1. `data/products.csv` に商品情報を用意する（`data/sample_products.csv` を参考に）。
2. 1商品ごとに次の順で回す：
   `kaitori-researcher` → `kaitori-blog-writer` / `kaitori-sns-writer` → `kaitori-editor`
3. `output/review/` の判定が「OK」になったら、WordPress や SNS へ投稿する（投稿は現状オフライン＝人手 or 別途連携）。

複数商品をまとめて処理したいときは、メインの対話で「CSVの各行に対してチームを回して」と指示すればオーケストレーションできる。

## ディレクトリ

```
.claude/agents/   … サブエージェント定義
data/             … 入力CSV
output/research/  … リサーチメモ
output/blog/      … ブログ記事（フロントマター付きMarkdown）
output/sns/       … SNS投稿案
output/review/    … 校正レポート
```

## 執筆ルール（全エージェント共通）

- 価格・買取金額は必ず「目安」「時期・状態で変動」と分かる書き方にする。
- 根拠のない最上級・断定（「最高値」「必ず」等）は使わない（景品表示法）。
- 化粧品・美容/健康の効果効能を断定しない（薬機法）。
- リサーチメモにない事実（価格・スペック）を創作しない。不足は `<!-- 要確認 -->` で残す。

## 配送・送料の表記ルール（全エージェント共通・出品記事は必須）

在庫商品を「出品」する記事・投稿（個体を特定して販売するブログ記事・SNS投稿）には、送料問い合わせの手間を減らすため、配送業者「アートセッティングデリバリー（家財おまかせ便）」の情報を必ず記載する。

- 案内・料金確認先URL: https://www.008008.jp/ （料金検索フォーム: https://form.008008.jp/mitumori/PKZI1100Action_doInit.action ）
- 対象商品の**サイズランク**（3辺合計サイズ〔cm〕で区分。下表参照）を明記する。ランクは商品情報（CSV・リサーチメモ等）に記載があればそれを使う。記載がなければ、外形寸法（幅+奥行+高さ）から下表に照らして判定し、判定できない場合は `<!-- 要確認: サイズランク -->` として空欄にせず必ず注記する。
- 送料の具体額（税込金額）は個体・エリアごとに検索が必要なため断定しない。「送料はサイズランク・お届け先エリアにより異なります。詳細・正確な金額は上記URLでご確認ください」という趣旨を明記する。
- 各ランクの正確なcm基準・料金額は捏造しない。下表以外の情報（税込金額等）が必要な場合は上記URL参照を案内するに留める。
- サービス内容は基本的に「梱包・搬入・開梱・設置・梱包材の回収」までを含むとされるが、厨房機器特有の水回り・電源接続等の対応可否は個体・現場条件により異なるため断定しない。「設置対応は業者・現場条件により異なるため、詳細は上記URLでご確認ください」等のヘッジを添える。

### サイズランク表（3辺合計サイズ）

| ランク | 3辺合計サイズ | 代表例 |
| --- | --- | --- |
| SS | 〜80cm | 電話機／ファックス／HDレコーダー |
| S | 〜120cm | 電気ポット／炊飯器 |
| A | 〜160cm | 衣装ケース／石油ストーブ／扇風機／エアコン（室内機） |
| B | 〜200cm | 押入ダンス／肘掛け椅子／座卓／エアコン（室外機） |
| C | 〜250cm | 全自動洗濯機／ドラム式洗濯機／学童机／カーペット（6畳・4.5畳）／単身用冷蔵庫／布団袋 |
| D | 〜300cm | ソファ（2人掛け）／自転車（22インチ）／食器棚 |
| E | 〜350cm | 家庭用冷蔵庫（大型）／シングルベッド（簡易型）／自転車（26インチ）／タンス／本棚 |
| F | 〜400cm | ダブルベッド／衣装タンス |
| G | 〜450cm | カウチソファー |

料金（税込）はお預かり先・お届け先の都道府県の組み合わせで変わるため、本表には含めない。必ず上記URLの料金検索フォームで確認する。

## 保証に関する表記ルール（全エージェント共通・出品記事は必須）

出品記事・投稿で「直接引き取り（お客様がご自身で店舗・現地まで来て引き取る方法）」を選択肢として案内する場合は、**直接引き取りはいかなる場合も保証の対象外**であることを必ず明記する。

- 対象は配送業者（アートセッティングデリバリー等）を利用せず、その場で商品を持ち帰る場合すべて。理由（動作確認のタイミング、持ち帰り後の破損リスク等）は限定せず、一律で保証対象外とする。
- 断定の中身（例外を作らないこと）は変えず、言い回しは丁寧・柔らかいトーンにする。例：「恐れ入りますが、配送業者をご利用いただかず直接お引き取りいただく場合は、理由にかかわらず保証の対象外とさせていただいております。あらかじめご了承くださいませ。」（「一切」「いかなる場合も」のような強い断定語は避け、「恐れ入りますが」「〜とさせていただいております」「あらかじめご了承ください」等のクッション言葉を添える）。
- 配送・送料セクション内、または直接引き取りに言及する箇所の近くに記載する。

## スラッグ命名

`<ブランド>-<型番 or 商品名>` を英数ハイフンで。例：`rolex-submariner-116610`, `iphone-13-128gb`。
research / blog / sns / review で同じスラッグを使い、商品を横断で辿れるようにする。
